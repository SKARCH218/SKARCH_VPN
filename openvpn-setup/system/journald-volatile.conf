# /etc/systemd/journald.conf.d/volatile.conf
# Keep system logs in RAM (/run) instead of the SD card to reduce write wear.
# Logs are cleared on every reboot - fine for this box (it reboots daily anyway).
[Journal]
Storage=volatile
RuntimeMaxUse=64M
RuntimeMaxFileSize=8M
ForwardToSyslog=no
